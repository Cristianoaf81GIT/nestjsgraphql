type Query {
  courses: [Course] 
}

type Course {
  id: String!
  name: String!
  description: String!
  duration: String!
}

type Mutation {
  createCourse(data: CourseInput): Course 
}

input CourseInput {
 id: String!
 name: String!
 description: String!
 duration: String!
}
